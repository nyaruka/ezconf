# these fields can be set via command line or env
my_int = 32
my_bool = true
my_datetime = 2018-04-03T05:30:00Z
my_log_level = "info"
my_ints = [10, 20, 30]
my_strings = ["foo", "bar"]

# nested fields can only be set from TOML
[nested]
nested_int = 64